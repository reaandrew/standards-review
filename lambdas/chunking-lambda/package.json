{
  "name": "chunking-lambda",
  "version": "1.0.0",
  "description": "Lambda function to chunk text using custom splitting algorithm",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.799.0",
    "aws-sdk": "^2.1502.0"
  }
}
